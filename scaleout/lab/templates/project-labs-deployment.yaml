apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-labs-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-labs-app
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-labs-app
        project: {{ .Values.project.name }}
        type: lab
    spec:
    {{ if .Values.labs.gpu.enabled }}
      affinity:
        {{ .Values.labs.affinity | toYaml | nindent 8 | trim }}
    {{ end }}
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      #securityContext:
      #  runAsUser: 65533  # to allow read of ssh key
      containers:
      - name: {{ .Release.Name }}-gatekeeper
        image: keycloak/keycloak-gatekeeper:latest #quay.io/gambol99/keycloak-proxy:latest
        imagePullPolicy: Always
        args:
          - --config=/etc/secrets/gatekeeper.yaml
          # # - --enable-default-deny=true
          # - --resources="uri=/*|groups=lab-group"
          # # - --resources=uri=/*|white-listed=true|methods=GET,PUT
          # # - --resources=uri=/lab/api/*|white-listed=true|methods=GET,PUT
          # - --discovery-url=https://keycloak.smart.stackn.dev/auth/realms/{{ .Values.gatekeeper.realm }}
          # - --client-id={{ .Values.gatekeeper.client_id }}
          # - --client-secret={{ .Values.gatekeeper.client_secret }}
          # - --listen=0.0.0.0:9090
          # - --enable-session-cookies
          # # - --enable-refresh-tokens=true
          # # - --encryption-key=AgXa7xRcoClDEU0ZDSH4X0XhL5Qy2Z2j
          # - --enable-logging=true
          # - --enable-json-logging=true
          # - --upstream-url=http://localhost:8888
        ports:
          - containerPort: 9090
        volumeMounts:
        - name: gatekeeper-config
          mountPath: /etc/secrets/
      - name: {{ .Release.Name }}-s3-sync
        image: scaleoutsystems/s3-sync:latest
        imagePullPolicy: Always
        securityContext:
          #runAsUser: 1000
          #runAsGroup: 100
          privileged: true
          capabilities:
            add:
              - SYS_ADMIN
        resources:
          limits:
            cpu: 300m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
        #lifecycle:
        #  postStart:
        #    exec:
        #      command: ["mkdir","/home/jovyan/dataset"]
        env:
        - name: BUCKET
          value: {{ .Values.minio.bucket | default "dataset" }}
        - name: MOUNTPOINT
          value: /data
        - name: REPOURL
          value: "http://{{ .Values.project.name }}-minio:9000"
        - name: AWSACCESSKEYID
          value: {{ .Values.minio.access_key }}
        - name: AWSSECRETACCESSKEY
          value: {{ .Values.minio.secret_key }}
        volumeMounts:
        - name: dataset
          mountPath: /data
          mountPropagation: Bidirectional
        - name: devfuse
          mountPath: /dev/fuse
      - name: {{ .Release.Name }}-labs-app
        image: {{ .Values.labs.image }}
        ports:
        - containerPort: 8888
        env:
        - name: JUPYTER_ENABLE_LAB
          value: "yes"
        - name: NOTEBOOK_PASSWD_SHA1
          value: ""
        - name: NOTEBOOK_TOKEN
          value: ""
        resources:
          requests:
            memory: {{ .Values.labs.resources.requests.memory | default "256Mi" }}
            cpu: {{ .Values.labs.resources.requests.cpu | default "200m" }}
            {{if .Values.labs.gpu.enabled }}
            nvidia.com/gpu: "1"
            {{ end }}
          limits:
            memory: {{ .Values.labs.resources.limits.memory | default "256Mi" }}
            cpu: {{ .Values.labs.resources.limits.cpu | default "200m" }}
            {{if .Values.labs.gpu.enabled }}
            nvidia.com/gpu: "1"
            {{ end }}
        volumeMounts:
        - name: homedir
          mountPath: /home/jovyan/
        - name: dataset
          mountPath: /home/jovyan/dataset/
          mountPropagation: HostToContainer
        - name: jupyterconf
          mountPath: /etc/jupyter/jupyter_notebook_config.py
          subPath: jupyter_notebook_config.py
        - name: projconfig
          mountPath: /home/jovyan/.scaleout/
        securityContext:
          allowPrivilegeEscalation: true
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      volumes:
      - name: dataset
        emptyDir: {}
      - name: homedir
        persistentVolumeClaim:
          claimName: {{ .Values.project.name }}-project-files
      - name: projconfig
        configMap:
          name: {{ .Values.project.name }}-project-settings-configmap
      - name: devfuse
        hostPath:
          path: /dev/fuse
      - name: gatekeeper-config
        configMap:
          name: {{ .Release.Name }}-gatekeeper-config

      - name: jupyterconf
        configMap:
          name: {{ .Release.Name }}-jupyter-configmap
