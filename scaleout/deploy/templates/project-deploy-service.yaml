apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appname }}
  labels:
    run: {{ .Values.appname }}
spec:
  ports:
  - port: 80
    targetPort: 9090
    protocol: TCP
  selector:
    app: {{ .Values.appname }}