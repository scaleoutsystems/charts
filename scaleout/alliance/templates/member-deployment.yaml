apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.20.0 ()
  creationTimestamp: null
  labels:
    io.kompose.service: {{ .Release.Name }}-member
  name: {{ .Release.Name }}-member
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.20.0 ()
      creationTimestamp: null
      labels:
        io.kompose.service: {{ .Release.Name }}-member
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      containers:
      - args:
        - sh
        - ./run.sh
        env:
        - name: GET_HOSTS_FROM
          value: dns
        - name: MINIO_HOST
          value: {{ .Release.Name }}-minio
        - name: CONTROLLER_HOST
          value: {{ .Release.Name }}-controller
        image: "{{ .Values.member.image.repository }}:{{ .Values.member.image.tag }}"
        name: {{ .Release.Name }}-member
        resources: {}
      #  volumeMounts:
      #  - mountPath: /app
      #    name: member-claim0
      #  - mountPath: /app/data
      #    name: member-claim1
      restartPolicy: Always
      #volumes:
      #- name: member-claim0
      #  persistentVolumeClaim:
      #    claimName: member-claim0
      #- name: member-claim1
      #  persistentVolumeClaim:
      #    claimName: member-claim1
status: {}
